# Indoor LLM Loop Closure

Minimal Python scaffold for an indoor RGB-D loop closure prototype.

## Layout

- `configs/` holds sample YAML configuration files.
- `src/indoor_loop/` contains the importable package.
- `tests/` contains configuration-loading tests.

## Configuration

Load a YAML config with `indoor_loop.config.load_config`.

